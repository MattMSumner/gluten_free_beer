development: &default
  adapter: postgresql
  database: gluten_free_beer_development
  encoding: utf8
  host: localhost
  min_messages: warning
  pool: 5
  timeout: 5000

test:
  <<: *default
  database: gluten_free_beer_test
